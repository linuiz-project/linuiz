name: Post-commit Validation

on:
  - pull_request

env:
  NIGHTLY_VERSION: nightly-2025-09-20

defaults:
  run:
    shell: bash

jobs:
  validation:
    name: Build & Formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/cache@v4
        id: cache
        with:
          path: |
            ~/.rustup/
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ env.NIGHTLY_VERSION }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Dependencies
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          rustup toolchain install --profile minimal --component rust-src,rustfmt,clippy,miri $NIGHTLY_VERSION
          rustup target add x86_64-unknown-none
          cargo install cargo-sort

      - name: Configure Environment
        run: rustup default $NIGHTLY_VERSION

      - name: Check Formatting
        run: cargo fmt --check

      - name: Check Dependency Ordering
        run: cargo sort --workspace --grouped --check

      - name: Check Clippy Lints
        run: cargo clippy -- -A clippy::todo -D warnings
      
      - name: Check Build Status
        run: cargo xtask build -t x86_64 --github-actions

      - name: Run Miri Tests
        run: cargo miri test

